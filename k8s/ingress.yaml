apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-dvwa
spec:
  defaultBackend:
    service:
      name: dvwa-web-service
      namespace: dvwa-group25-web
      port:
        number: 8080
